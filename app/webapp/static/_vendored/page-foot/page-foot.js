/* Fleet page-foot text contract (design.md v2 `page-foot` component).
 *
 * VENDORED VERBATIM from project-scaffolding. Do NOT edit this file per-app:
 * nothing here varies between apps. Normalized from home-automation's
 * main.js, where whatsapp-radar#122's first feedback item was precisely a
 * footer-format divergence from a private re-implementation — this file is
 * the one shared `fmtBuildTime`, written once.
 *
 * The caller still owns the actual fetch (GET /api/version is auth-gated —
 * attach the bearer via your own jsonApi) and any reload-on-stale-asset
 * logic; this module only formats the display text.
 */

export function fmtBuildTime(iso) {
  if (!iso) return '';
  const d = new Date(iso);
  if (Number.isNaN(d.getTime())) return String(iso).replace('T', ' ').slice(0, 16);
  const pad = function (n) { return String(n).padStart(2, '0'); };
  return d.getFullYear() + '-' + pad(d.getMonth() + 1) + '-' + pad(d.getDate()) +
    ' ' + pad(d.getHours()) + ':' + pad(d.getMinutes());
}

export function buildReadoutText(sha, builtAtIso) {
  const ts = fmtBuildTime(builtAtIso);
  return ts ? ('Build: ' + sha + ' · ' + ts) : ('Build: ' + sha);
}
