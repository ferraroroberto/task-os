# UX round 8 — the Tree folds into the Table, and the pill drops to five (issue #161)

Eighth on-screen review, and a structural one rather than a polish pass: the **Tree stops being a nav destination** and becomes the **Table tab's second view**, flipped by a two-state segmented control in the pane's own top strip. The nav pill goes from six entries to five — a slot the coming **Archive** tab ([#159](https://github.com/ferraroroberto/task-os/issues/159)) takes without crowding it.

The reasoning is the app's own: Board, Table, Tree, Today and the Search tab's task hits are already *renderings of one filtered list* (#46), and the Table and the Tree had reached the point of being byte-for-byte the same pane — the same top strip (text filter · select toggle · quick-add · bulk bar), the same filter card, the same rows — differing only in the host they drew into. Two drawings of one list are a **view switch**, not two destinations; the pill is for places you go.

Judged on the seeded disposable instance (synthetic data only, `tests/fixtures/seed.py`) at 1440×900 and 390×844, light and dark. Every screenshot below is a story shot captured through `shot()`, so this round is proven by the gallery the suite regenerates rather than by hand-captured extras.

Desktop: [the grid view, toggle on the left half](../screenshots/story-04-triage-8-desktop.png) · [the tree view, toggle on the right half](../screenshots/story-13-starts-snooze-2-desktop.png) · [the tree carrying a blocked task](../screenshots/story-20-blocked-by-2-desktop.png). Phone: [the strip at 390 px, four 44 px squares in a row](../screenshots/story-04-triage-9-phone.png) · [the five-entry pill](../screenshots/story-07-phone-1-phone.png).

## The round

| # | Change | Done | Seen in |
| --- | --- | --- | --- |
| 1 | Six nav buttons where two of them were the same pane drawn twice | `index.html`'s `tabTree` button and the whole `#paneTree` section are gone; `#treeHost` moved into `#paneTable`'s body beside `#tableHost`. The vendored `nav-tabs.js` is untouched — the tab list is markup, exactly as its README says, so removing a `<button data-tab>` and its `<section>` is the entire nav change | [desktop](../screenshots/story-04-triage-8-desktop.png), [phone pill](../screenshots/story-07-phone-1-phone.png) |
| 2 | No way to reach the tree once the tab is gone | a **two-button segmented control** (`table` / `list-tree` glyphs, `aria-pressed`, tooltips *Table* / *Tree*) in the Table pane's top strip, beside the Select toggle. Both halves are `.strip-square`, so the control is exactly the square its neighbours are at every width — 36 px on a fine pointer, 44 px on touch — and the pair joins by squaring the inner corners and dropping the second half's left hairline rather than by a negative margin, which would have made two 44 px hit rects overlap (design.md's adjacent-cluster rule). The pressed half wears the same `--accent-soft` tint the Select toggle wears when it is on: one "engaged" tone across the strip | [grid pressed](../screenshots/story-04-triage-8-desktop.png), [tree pressed](../screenshots/story-13-starts-snooze-2-desktop.png), [phone](../screenshots/story-04-triage-9-phone.png) |
| 3 | The choice has to survive a reload, or an installed PWA reopens on the wrong drawing | `localStorage` key `task-os.tableView` (`table` \| `tree`, default `table`); a tab **stored as `tree` by a build from before this round** is migrated on boot to `table` + the tree view, so an existing install lands where it left off instead of on the default tab. Both are walked in the e2e (`_walk_starts_and_snooze`) | — |
| 4 | *Go to Tree* named a destination that no longer exists | the palette command becomes **Table → Tree view** and sets the tab *and* the view; *Go to Table* sets the grid view for the same reason. Walked in story 10 | [palette](../screenshots/story-10-search-5-desktop.png) |
| 5 | The tree had its own filter card, text filter and bulk bar — a third copy of state that had to agree with the Table's | deleted. The tree now shares the Table's strip and card outright, so `renderFilters`, `renderSelectMode`, `wireSelectMode` and `renderNoTasks` each lost a branch instead of gaining one. `app.js` owns one `state.tableView` and one `renderTablePane()` that renders the active host and **empties** the other, so a query scoped to the pane never finds rows nobody can see | [tree](../screenshots/story-13-starts-snooze-2-desktop.png) |

## What deliberately did not change

- **`tree.js` and `table.js` are untouched.** How the tree and the grid draw a row was never the point of this round; where the tree lives was.
- **The Table tab keeps the id `table`.** Stored tabs, `#task/<id>` deep links and every story test that opens the Table keep working with no migration.
- **The tree keeps everything it had** inside the pane: drag re-parent, the persisted collapse state, the ↑↓→← outline walk (and its exemption from the row action keys — `keys.js`'s `NOT_A_TARGET` moved from `#paneTree` to `#treeHost`, the same rule keyed on the host rather than a pane that no longer exists), Select mode + the bulk bar, the quick-add `+` focusing the new row in whichever host is up, and its role as the one working view that still shows a **deferred** or **blocked** task.
- **The e2e suite gained no test.** Five inline `data-tab='tree'` clicks became one `tree_view(page)` helper in `tests/e2e/conftest.py` (plus its twin `table_view(page)` for the walks that come back to the grid); the suite stays at 16 collected across 13 files, exactly as before.

## Verification

- `& .\scripts\verify-before-ship.ps1` green: byte-compile · ruff · **509 unit** · e2e **full** tier (`app/webapp/` + `tests/e2e/` in the diff), **16 tests**, one pass, no reruns.
- `& .\.venv\Scripts\python.exe -m scripts.shot_determinism` green: 173 shots, 161 byte-identical across two runs of this commit, 6 raster-noise only (≤1/255), 6 on the stated allowlist, **0 moved**. The gallery moved because the nav pill is on every shot — a real, reviewable delta, not noise.
- **Headed Chromium walk** on the seeded disposable instance, driving the real thing and read back shot by shot: five nav tabs and no `tree` among them; Table → grid, toggle → tree, the grid host hidden *and* emptied; a drag re-parent inside the pane (toast *Moved "Call the plumber back" under Return library books*, confirmed on the API); the collapse toggle and the ↑↓ outline walk still on the tree rows; a reload coming back on the tree with `task-os.tableView = tree`; both views in **dark**; the toggle stepping aside when the bulk bar takes the strip over; quick-add focusing the new row *inside the tree host*; and the palette's *Table → Tree view* landing on the tab and the view together. Zero `pageerror`s across the whole walk.
- **Independent review** (a fresh agent with no memory of the build, re-running the gate itself): pass. Its one substantive finding was fixed in-branch before shipping — on a still-empty install the toggle is hidden, but the palette can still switch the view, and the host it revealed came up blank because the re-render was gated on `state.total` being truthy. `setTableView` now renders the first-task prompt when the total is a confirmed `0`, and does nothing while it is still `null` (unknown, the pending refresh renders). Story 01 asserts it, and the assertion was proven to **fail** against the pre-fix code first.
- Geometry proven, not eyeballed: story 04's phone leg now asserts `assert_min_target` **and** `assert_no_overlap` over `#paneTable .filter-q, .view-seg, .quick-add-btn` — the segmented halves touch on a shared hairline and their effective rectangles still do not intersect.
- Tokens read back against `~/.claude/design.md` + `design.dark.md`: the control uses only `--line`, `--card-off`, `--radius-md`, `--control-h` / `--row-sm`, `--icon-title`, `--accent-soft`, `--accent-border-soft` and `--accent` — every one already defined in both themes, no new custom property and no literal colour. The vendored nav is byte-identical.
- Not verified here: the same walk on a **real phone** over the tailnet (owner-only — the standing item in [validation.md](../validation.md)'s owner checklist).
