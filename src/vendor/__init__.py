"""Modules vendored **verbatim** from other repos — never edited here.

``foldersearcher_core.py`` comes from ``automation/system/foldersearcher``
(GUI-free half of the folder searcher); source path + commit are recorded in
``.fleet.toml`` ``[vendored]``. Re-vendor by copying the file again and
bumping the sha — a local fix belongs upstream.
"""
